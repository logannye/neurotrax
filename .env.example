# The stable prototype has no runtime environment variables. It is a static
# application and performs its default analysis locally in memory.
# The optional WavLM research service is configured separately and is not a
# browser dependency; see services/voice-inference/README.md.
