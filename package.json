{
  "name": "phenometric",
  "version": "0.1.0",
  "private": true,
  "description": "A clinical observability prototype for quality-aware face and voice measurement across telehealth encounters.",
  "license": "MIT",
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "pnpm --filter @phenometric/capture-web dev",
    "demo:smoke": "pnpm --filter @phenometric/capture-web demo:smoke",
    "build": "pnpm -r --if-present run build",
    "check": "bash scripts/validate-structure.sh",
    "test:unit": "pnpm -r --if-present run test:unit",
    "test:browser": "pnpm --filter @phenometric/capture-web test:browser",
    "test:python": "uv run --project services/voice-inference --extra dev pytest services/voice-inference/tests",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "npm run check && npm run test:unit && npm run typecheck && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
