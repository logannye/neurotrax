{
  "name": "neurotrax",
  "version": "0.1.0",
  "private": true,
  "description": "A demo-first agentic audiovisual sidecar for longitudinal tele-neurology.",
  "license": "MIT",
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "check": "bash scripts/validate-structure.sh",
    "test:unit": "pnpm -r --filter @neurotrax/ambient-core test:unit",
    "typecheck": "pnpm -r --filter @neurotrax/ambient-core typecheck",
    "test": "npm run check && npm run test:unit && npm run typecheck"
  },
  "engines": {
    "node": ">=22"
  }
}
