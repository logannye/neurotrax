# Clinician review surface

No separate clinician-review application is implemented.

The current browser renders a deterministic, session-only measurement report.
It does not contain generated narrative, accept/reject controls, durable review
state, trajectory, persistence, export, or EHR integration. This directory is
retained only to mark the future boundary of the third named capability.
